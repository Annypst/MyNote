**码元（Symbol）**：是一个具体的**物理信号波形**或**状态实体**。

**波特（Baud）**：是一个**频率/速率单位**，表示“每秒传输的码元个数”，即：

$$1 \text{ Baud} = 1 \text{ 码元/秒}$$

$$\text{比特率 (bit/s)} = \text{波特率 (Baud)} \times \text{每个码元携带的比特数 (bit/码元)}$$